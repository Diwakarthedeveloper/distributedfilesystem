package main

import "fmt"

func main() {
	fmt.Print("Hey this is server1")
}

// we will do peer to peer file transfer where we send the file and that file is hashed  so we ill use hash as a key , create folders to do versioning of that file, cache
